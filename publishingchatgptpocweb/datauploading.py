from core.config import PathConfig, settings
from scripts.scaledb import ScaleDB
from core.config import PathConfig, settings
from scripts import ai, file

if __name__ == '__main__':
    # scale_db_instance = ScaleDB(settings, PathConfig)
    # scale_db_instance.create_vector_data_from_processed_documents(type='articles')
    # scale_db_instance.remove_unreferenced_concept_nodes()
    input("Press Enter to exit...")